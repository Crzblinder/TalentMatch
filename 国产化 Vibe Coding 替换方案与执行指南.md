# 国产化 Vibe Coding 替换方案与执行指南

## ⚠️ 方案合理性评估与“幻觉”纠偏

在将此方案直接应用于 Cursor、Windsurf 等 AI 编程工具（Vibe Coding）之前，必须对原方案进行“脱水”和纠偏。原方案中存在部分 AI 生成的“幻觉”（虚构的开源包和库），如果直接使用，会导致 AI 编造不存在的代码，项目无法运行。

### 需要剔除或修正的“幻觉”与不合理项

1.  **虚构的 MCP 包**：`@iflow-mcp/mergedao-mcp-jobs` 在 NPM 和 GitHub 上并不存在。MCP（模型上下文协议）目前最稳妥的方式是使用官方 SDK 自建 MCP Server。
2.  **虚构的开源库**：`tavily-open`、`resume-jd-optimizer-cn`、`skillnet-ai` 大概率为 AI 虚构的包名，直接 `pip install` 或 `npm install` 会报错。
3.  **网络冲突项**：`Apify` 是国外平台，国内直连极不稳定，违背了“无需代理、国内直连”的初衷。
4.  **大模型选型**：盘古和元景的 Function Calling 生态和文档开放度不如阿里云和智谱，建议聚焦头部厂商。

---

## ✅ 修正后的 100% 真实可用技术选型

为了确保 Vibe Coding 一次成功，请使用以下真实存在且国内直连的技术栈：

| 模块 | 推荐真实方案 | 说明 |
| :--- | :--- | :--- |
| **联网搜索** | **博查 (Bocha) API** / **智谱 Web Search** | 博查是专为 AI Agent 打造的国内搜索 API，结构化极好；智谱搜索质量高。 |
| **开源搜索** | **SearXNG** (Docker 部署) | 真实存在的开源元搜索引擎，国内 Docker 部署，完全免费。 |
| **MCP 服务器** | **自建 MCP Server** (Python/TS SDK) | 使用 `mcp` (Python) 或 `@modelcontextprotocol/sdk` (TS) 封装真实的招聘网站爬虫（如 Playwright）。 |
| **简历解析** | **通义千问文档解析 API** / **Doc2X** | 阿里云百炼提供强大的文档理解 API；Doc2X 是国内优秀的文档转结构化 JSON 服务。 |
| **大模型 FC** | **阿里云百炼 (Qwen-Max)** / **智谱 GLM-4** | 这两家的 Function Calling 能力、中文理解和 API 稳定性在国内属于第一梯队。 |
| **技能图谱** | **大模型动态提取** + **真实招聘平台字典** | 放弃虚构图谱，直接通过 Prompt 让 Qwen 提取技能标签，或调用 Boss/猎聘的公开 API 获取技能字典。 |

---

## 🚀 Vibe Coding 执行步骤 (SOP)

在使用 AI 辅助编程时，建议将项目拆分为以下 4 个 Phase，逐步让 AI 生成代码，避免上下文混乱。

### Phase 1: 基础设施与 MCP 工具链搭建

- **Prompt 策略**：让 AI 帮你搭建本地环境和 MCP Server。
- **执行动作**：
    1.  初始化项目（如 Python FastAPI 或 Node.js Express）。
    2.  使用 Docker 部署本地 `SearXNG` 实例作为备用搜索。
    3.  **核心**：让 AI 编写一个 Python MCP Server，封装 `Playwright` 或 `requests`，实现对目标招聘网站（如 Boss直聘、智联）的公开数据抓取，并暴露为 `search_jobs` 工具。
    4.  注册博查 API 或智谱搜索 API，封装为 `web_search` 工具。

### Phase 2: 核心 Agent 与 Function Calling 接入

- **Prompt 策略**：让 AI 编写大模型路由和工具调用逻辑。
- **执行动作**：
    1.  接入阿里云百炼（Qwen）API，配置 System Prompt。
    2.  将 Phase 1 的 MCP 工具转换为 Qwen 支持的 `tools` (JSON Schema) 格式。
    3.  编写 Agent 循环逻辑（ReAct 模式）：接收用户输入 -> 模型判断调用工具 -> 执行工具 -> 返回结果给模型 -> 生成最终回答。

### Phase 3: 简历解析与业务流串联

- **Prompt 策略**：处理非结构化数据，串联“解析-匹配-优化”链路。
- **执行动作**：
    1.  接入通义千问文档解析 API，实现 PDF/Word 简历转 Markdown/JSON。
    2.  编写“技能提取 Prompt”，从简历和 JD 中分别提取核心技能列表。
    3.  编写“匹配度计算与优化建议 Prompt”，让大模型对比两者差异，输出修改建议。

### Phase 4: 前端交互与流式输出

- **Prompt 策略**：构建用户界面，处理 SSE（Server-Sent Events）。
- **执行动作**：
    1.  使用 Vue/React 或 Streamlit/Gradio 快速搭建前端。
    2.  后端实现流式输出（Streaming），让用户能看到 AI 思考和调用工具的过程。

---

## 🧪 测试流程

为确保 Agent 不会“发疯”或陷入死循环，需建立严格的测试漏斗：

### 1. 单元测试 (Unit Testing)

- **工具可用性测试**：单独调用 MCP 的 `search_jobs` 和 `web_search`，断言返回的 JSON 结构完整，无反爬拦截（如 403/503 错误）。
- **解析准确率测试**：准备 10 份不同排版（单栏、双栏、含表格）的真实简历，调用解析 API，断言关键字段（姓名、学历、工作经历）提取准确率 > 90%。

### 2. 集成测试 (Integration Testing)

- **Function Calling 路由测试**：输入特定指令（如“帮我找珠海的Java岗位”），断言大模型准确生成了工具调用参数（如 `{"location": "珠海", "keyword": "Java"}`），且没有产生幻觉调用不存在的工具。
- **上下文窗口测试**：输入超长 JD 和简历，确保不会触发大模型的 Token 超限报错（需做截断或分块处理）。

### 3. 端到端测试 (E2E Testing)

- **黄金链路测试**：模拟真实用户操作：`上传简历` -> `输入目标岗位` -> `系统自动搜索岗位` -> `输出匹配度报告` -> `输出简历修改建议`。
- **异常兜底测试**：在搜索 API 超时或招聘网站反爬时，断言 Agent 能优雅降级（如提示用户手动输入 JD），而不是直接抛出代码异常。

---

## 🎯 验收标准 (Acceptance Criteria)

项目交付或上线前，必须满足以下标准：

### 1. 功能验收

- [ ] **全链路闭环**：系统能成功接收简历，自动联网搜索相关岗位，并输出针对性的简历优化建议。
- [ ] **工具调用透明**：前端能清晰展示 Agent 的思考过程（Thought）和工具调用状态（Action/Observation）。
- [ ] **多格式支持**：支持 `.pdf`, `.docx`, `.txt` 格式的简历上传与解析。

### 2. 性能与体验验收

- [ ] **首字响应时间**：流式输出的首字响应时间（TTFT） `< 1.5秒`。
- [ ] **工具执行耗时**：单次联网搜索或岗位抓取耗时 `< 5秒`。
- [ ] **并发处理**：系统能同时处理至少 10 个用户的并发请求而不崩溃（需引入异步队列如 Celery 或 Redis）。

### 3. 国产化与合规验收（核心）

- [ ] **100% 国内直连**：在关闭所有代理软件的环境下，所有 API 请求和网页抓取均能成功，无网络超时。
- [ ] **数据隐私合规**：用户上传的简历在解析和比对完成后，必须进行脱敏处理或定期物理删除，不违规留存个人隐私数据（符合《个人信息保护法》）。
- [ ] **内容安全**：接入阿里云/百炼的内容安全审核 API，确保 Agent 输出的优化建议不包含违规、歧视性言论。

---

> 💡 **Vibe Coding 终极建议**：
> 在使用 Cursor 等工具时，请将上述“修正后的技术选型”和“执行步骤”作为 `.cursorrules` 或项目根目录的 `CONTEXT.md` 文件喂给 AI。明确告诉 AI：“不要使用任何虚构的第三方库，所有 API 调用必须基于官方最新文档，遇到不确定的库请先向我确认。” 这样能最大程度提高代码的一次性通过率。