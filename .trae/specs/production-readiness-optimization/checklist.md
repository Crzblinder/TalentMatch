# Checklist

## 后端基础设施 (Phase 1)

### 简历上传与解析
- [x] `POST /api/v1/resumes/upload` 端点可正常接受 PDF 文件上传
- [x] `POST /api/v1/resumes/upload` 端点可正常接受 DOCX 文件上传
- [x] 上传不支持格式时返回 400 错误
- [x] 上传后返回结构化技能画像（技能列表、经验级别、学历）
- [x] 大文件（>10MB）上传有合理处理

### 技能名称归一化
- [x] "Reactjs" 输入可匹配到 aliases 中的 "React" 标准名
- [x] 直接匹配标准名不受影响
- [x] 无匹配别名时保留原始名称
- [x] 归一化同时适用于用户输入技能和岗位要求技能

### 匹配算法升级
- [x] 技能权重正确影响匹配分数计算
- [x] 经验年限匹配逻辑正确（高于/等于/低于要求的得分不同）
- [x] 学历匹配逻辑正确
- [x] 匹配结果包含 `experience_match` 和 `education_match` 字段
- [x] 无 LLM 降级模式也使用升级后的算法

## 后端业务增强 (Phase 2)

### 岗位智能推荐
- [x] `GET /api/v1/profiles/{id}/recommendations?top_n=20` 返回正确结果
- [x] 返回结果按匹配分数降序排列
- [x] 不存在的 profile_id 返回 404
- [x] 岗位库为空时返回空列表 + 提示信息
- [x] 支持 top_n 参数控制返回数量

### 技能图谱缓存
- [x] 首次调用时从 DB 构建图谱
- [x] 后续调用直接使用缓存，无额外 DB 查询
- [x] 技能数据变更后缓存正确失效
- [x] 缓存不影响匹配结果正确性

### 定时岗位数据采集
- [x] 系统启动后定时任务自动运行
- [x] 每 6 小时执行一次（或配置的间隔）
- [x] 采集失败不阻塞系统正常使用
- [x] `POST /api/v1/crawler/trigger` 可手动触发
- [x] `SCHEDULER_ENABLED=false` 时定时任务不启动

## 前端设计系统重构 (Phase 3)

### 工程化升级
- [x] Tailwind CSS 正确配置并生效
- [x] shadcn/ui 组件可正常导入使用
- [x] 旧 CSS 与 Tailwind 无样式冲突
- [x] 全局布局（Sidebar + Main）功能正常
- [x] 路由导航正常工作

### 仪表盘
- [x] 统计卡片使用 shadcn/ui Card 组件
- [x] 加载中显示 Skeleton 而非纯文字
- [x] 数据为空时显示引导性文案
- [x] 图表正常渲染

### 岗位匹配页
- [x] 步骤指示器正确显示当前步骤
- [x] 画像输入、岗位选择、匹配结果三步骤流程完整
- [x] 匹配分数视觉突出，颜色随分数变化
- [x] 技能对比雷达图正常渲染
- [x] 流式匹配进度条正常显示

### 岗位库页
- [x] 表格列头点击可排序
- [x] 排序状态有视觉反馈（箭头指示）
- [x] 岗位详情抽屉正常打开/关闭
- [x] 搜索+筛选功能正常

### 趋势分析 + 技能图谱
- [x] 趋势分析图表正常渲染
- [x] 技能图谱支持力导向布局
- [x] 图谱支持缩放（滚轮）
- [x] 图谱支持拖拽节点
- [x] 点击节点可展开新关联图谱

## 前端业务增强 (Phase 4)

### 新手引导
- [x] 首次访问（无画像）时弹出引导
- [x] 引导包含 3 个步骤，可前进/后退
- [x] 完成引导后可正常使用应用
- [x] 已有画像用户不显示引导
- [x] 刷新页面引导状态保持

### 岗位收藏
- [x] 收藏按钮在岗位列表和详情中可见
- [x] 点击收藏后按钮状态切换为已收藏
- [x] 再次点击取消收藏
- [x] 收藏列表页可查看所有收藏
- [x] 后端 API 返回正确的收藏数据

### 岗位对比
- [x] 岗位列表支持多选（最多 3 个）
- [x] 点击"对比"打开对比面板
- [x] 对比面板并排显示所有维度
- [x] 关闭对比面板后选择状态清除

### 匹配结果导出
- [x] 导出按钮在匹配结果面板可见
- [x] 点击导出生成 PDF 文件
- [x] PDF 包含匹配分数、技能对比、分析摘要
- [x] PDF 下载文件命名合理（含时间戳）
